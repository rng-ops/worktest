# Alignment & Segmentation Settings
THRESHOLD=0.70                    # Membership threshold (0.0 - 1.0)
EPOCH_SECONDS=60                  # Epoch rotation interval (seconds)
MAX_BENCHMARK_AGE=120             # Max age of benchmark before expiry (seconds)

# Network
CONTROLLER_PORT=8000              # Controller HTTP port
WG_SUBNET=10.10.0.0/24           # WireGuard tunnel subnet
WG_LISTEN_PORT=51820             # WireGuard listen port

# Node Configuration
NUM_NODES=3                        # Number of nodes in demo
NODE_A_SCORE_MEAN=0.90           # Node-A benchmark score mean
NODE_B_SCORE_MEAN=0.85           # Node-B benchmark score mean
NODE_C_SCORE_MEAN=0.40           # Node-C benchmark score mean (low; will quarantine)

# Logging
LOG_LEVEL=INFO                     # DEBUG, INFO, WARNING, ERROR
